import MarketingPage from './MarketingPage';
import Registry from '@ui-autotools/registry';

const metadata = Registry.getComponentMetadata(MarketingPage);

metadata.exportedFrom({
  path: 'src/MarketingPage/MarketingPage.js',
});
